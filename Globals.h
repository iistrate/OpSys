#ifndef GLOBALS_H
#define GLOBALS_H

namespace Globals {
	const int FPS_CAP = 90;
}

enum WINDOW {
	SCREEN_WIDTH = 1200,
	SCREEN_HEIGHT = 900
};

enum CONTROLS {
	QUIT,
	DEBUG_MODE,
	CAMERA_MODE,
	LEFT_CLICK,
	RIGHT_CLICK
};

#endif